# Development / test environment
spring.datasource.url=${DEV_DB_URL:jdbc:mysql://localhost:3306/xhs_plugin_db?useSSL=false&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8}
spring.datasource.username=${DEV_DB_USERNAME:root}
spring.datasource.password=${DEV_DB_PASSWORD:change_me}

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
