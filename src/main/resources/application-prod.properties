# Production environment
spring.datasource.url=${PROD_DB_URL:jdbc:mysql://localhost:3306/xhs_plugin_db?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8}
spring.datasource.username=${PROD_DB_USERNAME:root}
spring.datasource.password=${PROD_DB_PASSWORD:change_me}

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
